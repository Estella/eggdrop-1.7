# Makefile for src/mod/dns.mod/
# $Id: Makefile.in,v 1.3 2004/08/27 09:34:11 wcc Exp $

RESLIB = @RESLIB@
RESINCLUDE = @RESINCLUDE@
srcdir = .


doofus:
	@echo ""
	@echo "Let's try this from the right directory..."
	@echo ""
	@cd ../../../ && make

static: ../dns.o
	@echo "$(RESLIB)" >> ../mod.xlibs

modules: ../../../dns.$(MOD_EXT)

../dns.o:
	$(CC) $(CFLAGS) $(CPPFLAGS) $(RESINCLUDE) -DMAKING_MODS -c $(srcdir)/dns.c
	@rm -f ../dns.o
	mv dns.o ../

../../../dns.$(MOD_EXT): ../dns.o
	$(LD) -o ../../../dns.$(MOD_EXT) ../dns.o $(RESLIB) $(XLIBS) $(MODULE_XLIBS)
	$(STRIP) ../../../dns.$(MOD_EXT)

depend:
	$(CC) $(CFLAGS) -DMAKING_DEPEND -MM dns.c > .depend

clean:
	@rm -f .depend *.o *.$(MOD_EXT) *~

distclean: clean
	@rm -f Makefile config.cache config.log config.status

#safety hash
